name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: test
    permissions:
      contents: read
      deployments: write
      issues: write
    steps:
      - uses: actions/checkout@v5

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          # Create the Pages project on first run; no-op (and don't fail) if it already exists.
          preCommands: npx wrangler pages project create dartpage --production-branch=main || echo "Pages project 'dartpage' already exists — continuing"
          command: pages deploy public --project-name=dartpage

      - name: Notify on deploy failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Deploy failed — ${new Date().toISOString().split('T')[0]}`,
              body: [
                '## Deploy Failure',
                '',
                `**Run:** ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
                `**Branch:** ${context.ref}`,
                `**Commit:** ${context.sha}`,
              ].join('\n'),
              labels: ['bug']
            })

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 20

      - run: npm install

      - run: npm test
